interface:
  display_name: "Meme Template Image Producer"
  short_description: "Batch-produce, approve, and upload meme template images"
  default_prompt: "Use $meme-template-image-producer to batch-produce replacement-based template images, show both approvals in chat, and upload approved PNGs."

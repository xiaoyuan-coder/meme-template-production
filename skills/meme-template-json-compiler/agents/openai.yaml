interface:
  display_name: "Meme Template JSON Compiler"
  short_description: "Compile uploaded images directly into Gallery v2 JSON"
  default_prompt: "Use $meme-template-json-compiler to compile uploaded approved template images and deliver final Gallery v2 JSON to the data dashboard."
